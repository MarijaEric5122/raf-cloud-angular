spring.application.name=backend-MarijaNatasa

# URL baze - localhost:3306 je standard, a raf_oblak je ime baze koju smo napravili
spring.datasource.url=jdbc:mysql://localhost:3306/raf_oblak?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# Hibernate pode?avanja
# update zna?i: Spring ?e sam napraviti tabele ako ne postoje ili ih dopuniti
spring.jpa.hibernate.ddl-auto=update

# Da u konzoli vidi? ?ta Spring kuca u bazu (korisno za debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect